[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

# Metadata to indicate this should not use WASM target
[package.metadata]
default-target = "native"

[dependencies]
clap = { workspace = true }
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = "1.0"
grex = "1.4"
